{
  "name": "outputparser",
  "version": "1.0.0",
  "description": "Triggers and Capability Output Parser",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev server/index.ts",
    "start": "node dist/index.js",
    "build": "tsc -p ./server --outDir ./dist && npm run build:ui",
    "build:ui": "cd ui && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "Quabbly"
  ],
  "author": "OvieMudi",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.8",
    "ts-node": "^10.5.0",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "dotenv": "^12.0.3",
    "fastify": "^3.25.3",
    "fastify-formbody": "^5.2.0",
    "fastify-static": "^4.5.0"
  },
  "engines": {
    "node": ">=14.19.0"
  }
}
